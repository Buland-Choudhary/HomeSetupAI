# Copy to .env.local for local dev; set the same names in Vercel for deploys.
OPENAI_API_KEY=

# Optional: web lookups fall back to OpenAI search if empty
EXA_API_KEY=

# Optional: override the Realtime model (default gpt-realtime-2.1)
OPENAI_REALTIME_MODEL=
